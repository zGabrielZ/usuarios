spring:
  profiles:
    active:
      - ${APP_PROFILE:dev}
  jpa:
    open-in-view: false
  output:
    ansi:
      enabled: ALWAYS

server:
  servlet:
    context-path: '/api'

springdoc:
  default-produces-media-type: application/json